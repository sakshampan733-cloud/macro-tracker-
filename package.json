{
  "name": "basal",
  "version": "1.0.0",
  "private": true,
  "description": "Native shell for Basal. The app itself lives in docs/ and needs no build step.",
  "scripts": {
    "ios": "cap sync ios && cap open ios",
    "sync": "cap sync ios"
  },
  "dependencies": {
    "@capacitor/cli": "^8.5.1",
    "@capacitor/core": "^8.5.1",
    "@capacitor/ios": "^8.5.1",
    "capacitor-health": "^8.2.0"
  }
}
